/**
 * Configuración de la reserva de sesiones.
 * La página /sesion habla SOLO con el microservicio propio (agenda.cogniplex.es);
 * la clave del motor de citas nunca llega al navegador.
 */
export const agendaApi = 'https://agenda.cogniplex.es/api';
